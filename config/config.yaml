# JOB CONFIGURATION

seed: 42

# centralized training config
batch_size: 256
val_split: 0.1
num_workers: 2
learning_rate: 0.003214
weight_decay: 0.00005743
momentum: 0.96
t_max: 50
epochs: 50
checkpoint_path: ""           # Leave blank "" to train from scratch, or set to "checkpoints/<checkpoint_filename>.pth" to resume
out_checkpoint_dir: "/content/drive/MyDrive/DL_project/checkpoints/dense_training_branch"


# federated training config
NUM_CLIENTS: 100
CLIENT_FRACTION: 0.1  # 10% of clients selected each round
LOCAL_EPOCHS: 4
BATCH_SIZE: 32
LR: 0.01
ROUNDS: 100
IID: True  # Set to False for non-iid
NC:   # Number of classes per client in non-iid setting
SEED: 42
